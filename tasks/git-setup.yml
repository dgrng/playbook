- name: Git setup name
  git_config:
    name: user.name
    scope: global
    value: "Mandip Gurung"
  tags:
    - git
- name: Git setup email
  git_config:
    name: user.email
    scope: global
    value: "{{ email }}"
  tags:
    - git
